apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: mealie
spec:
  parentRefs:
  - kind: Gateway
    name: nginx-gateway
    namespace: nginx-gateway
  hostnames:
  - "mealie.robertharte.net"
  rules:
  - backendRefs:
    - name: mealie
      port: 9000
