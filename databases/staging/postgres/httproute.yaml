apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: postgres
spec:
  parentRefs:
  - name: traefik-gateway
    sectionName: http
    namespace: traefik-gateway
    kind: Gateway
  hostnames:
  - postgres.robertharte.net
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: postgres
      port: 5432
