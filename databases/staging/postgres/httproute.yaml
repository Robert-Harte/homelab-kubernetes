apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: postgres
spec:
  parentRefs:
  - kind: Gateway
    name: nginx-gateway
    namespace: nginx-gateway
  hostnames:
  - "postgres.robertharte.net"
  rules:
  - backendRefs:
    - name: postgres
      port: 5432
